# Smoke Calculator

Android приложение для отслеживания выкуренных сигарет с чистой архитектурой (Clean Architecture).

## Возможности

### Основной функционал:
- 🚬 Кнопка фиксации выкуренной сигареты с временем
- 📊 Отображение количества оставшихся сигарет (может быть выше лимита)
- ⏱️ Таймер от последней выкуренной сигареты
- 📈 Статистика: сегодня, средняя за день, средняя за неделю, средняя за месяц

### Настройки:
- 💰 Цена на сигареты (валюта AZN)
- 📦 Размер пачки (по умолчанию 20 штук)
- 🎯 Целевое количество сигарет в день
- 🕐 Время начала дня (по умолчанию 5 утра)
- ⏳ Желаемый интервал между сигаретами (минуты)

### Виджет:
- 📱 Виджет 2x1 для домашнего экрана
- Показывает количество за сегодня
- Отображает таймер с последней сигареты
- Кнопка быстрой фиксации выкуренной сигареты
- Кнопка открытия приложения

## Архитектура

Проект использует современные практики разработки Android:

### Clean Architecture:
- **Data Layer**: Room Database, DataStore Preferences, Repository Implementation
- **Domain Layer**: Use Cases, Repository Interfaces, Models
- **Presentation Layer**: Jetpack Compose UI, ViewModels (MVVM)

### Технологии:
- **Kotlin** - язык программирования
- **Jetpack Compose** - современный UI toolkit
- **Room** - база данных SQLite
- **DataStore** - хранение настроек
- **Hilt** - Dependency Injection
- **Coroutines & Flow** - асинхронное программирование
- **Material 3** - дизайн система
- **Navigation Compose** - навигация

## Сборка проекта

### Требования:
- JDK 17 или выше
- Android SDK (API 26+)
- Android Studio Hedgehog или новее (рекомендуется)

### Шаги для сборки:

1. Склонируйте репозиторий:
```bash
git clone <repository-url>
cd smokeCalculator
```

2. Откройте проект в Android Studio

3. Дождитесь синхронизации Gradle

4. Соберите проект:
```bash
./gradlew assembleDebug
```

5. APK файл будет создан в:
```
app/build/outputs/apk/debug/app-debug.apk
```

### Сборка через командную строку (без Android Studio):

Если у вас нет Android Studio, но есть Android SDK:

1. Установите переменную окружения ANDROID_HOME:
```bash
export ANDROID_HOME=/path/to/android/sdk
```

2. Соберите проект:
```bash
./gradlew assembleDebug
```

## Установка APK

После сборки установите APK на устройство:

```bash
adb install app/build/outputs/apk/debug/app-debug.apk
```

Или скопируйте APK на устройство и установите вручную.

## Использование виджета

1. Долгое нажатие на домашнем экране
2. Выберите "Виджеты"
3. Найдите "Smoke Calculator"
4. Перетащите виджет на домашний экран

## Структура проекта

```
app/src/main/java/com/smokecalculator/
├── data/
│   ├── database/        # Room Database (Entities, DAOs)
│   ├── preferences/     # DataStore для настроек
│   └── repository/      # Реализация репозитория
├── domain/
│   ├── model/          # Модели данных
│   ├── repository/     # Интерфейсы репозитория
│   └── usecase/        # Use Cases (бизнес-логика)
├── presentation/
│   ├── main/           # Главный экран
│   ├── settings/       # Настройки
│   ├── statistics/     # Статистика
│   ├── navigation/     # Навигация
│   └── theme/          # Темы и стили
├── widget/             # Виджет для домашнего экрана
└── di/                 # Dependency Injection модули
```

## Лицензия

MIT License
